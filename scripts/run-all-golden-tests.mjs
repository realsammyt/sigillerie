#!/usr/bin/env node
// Runs every golden test for completed phases. Skips phases with status: pending in .archon/project.yaml.
// TODO populate. Phase 11 fills this out.
console.log('run-all-golden-tests: stub');
process.exit(0);
